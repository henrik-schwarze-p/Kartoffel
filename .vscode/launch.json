{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run SDL2 app",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main",
      "args": [],
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb",

      "preLaunchTask": "build-sdl2"  
    }
  ]
}
